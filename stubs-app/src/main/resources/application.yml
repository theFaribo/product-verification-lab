server:
  port: ${SERVER_PORT:8090}
  shutdown: graceful

spring:
  application:
    name: product-verification-stubs

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics

  endpoint:
    health:
      probes:
        enabled: true